
## v27.6.0 — ساختار حرفه‌ای‌تر صفحه‌ی سابسکریپشن

- **راهنمای اتصال سریع (۳ قدم):** یک پنل جدید به صفحه‌ی `/subscription/{uuid}` اضافه شد که دقیقاً توضیح می‌دهد کاربر چطور از صفر به اتصال برسد (نصب اپ → وارد کردن لینک → بروزرسانی و اتصال).
- **دکمه‌های «افزودن سریع» (Deep Link):** به‌جای اینکه کاربر مجبور باشه لینک رو دستی کپی/پیست کنه، حالا دکمه‌های مستقیم برای v2rayNG، Hiddify، sing-box و Streisand (iOS) اضافه شدن که با یک لمس، ساب مستقیم داخل اپ افزوده می‌شود. لینک‌های قبلی (دانلود از GitHub، کپی دستی، QR) هم دست‌نخورده باقی موندن؛ این دکمه‌ها فقط یک میان‌بر اضافه‌اند، جایگزین راه‌های قبلی نیستن.
- ساختار ستون کناری (Sidebar) بازچینی شد: راهنمای اتصال بالاتر از پروفایل اتصال قرار گرفت تا مسیر منطقی «اول چطور وصل شم» → «جزئیات فنی» → «کلاینت پیشنهادی» حفظ بشه.
- تمام ۲۸ متغیر جایگزین‌شونده‌ی این صفحه (`__LABEL__`, `__QR__`, `__QA_V2RAYNG__` و…) با اسکریپت بررسی شدند تا هیچ‌کدام بدون مقدار باقی نمانند؛ `node --check` روی اسکریپت این صفحه هم اجرا شد.


- **مدیریت ادمین‌ها حرفه‌ای شد:** جدول ساده قبلی حذف شد و به کارت‌های حرفه‌ای تبدیل شد (آواتار، نشان طلایی Owner در برابر نشان بنفش Admin، وضعیت زنده، چیپ‌های دقیق دسترسی هر ادمین). هیچ منطق/تابعی تغییر نکرد؛ فقط ظاهر.
- **رفع باگ واقعی TCP/WS در ویرایش اینباند:** ریشه‌ی باگ پیدا شد — تابع رندر کارت‌های ترنسپورت (`manualBuilderHtml`) و تابع همگام‌سازی فیلد پنهان بعد از باز شدن Drawer ویرایش، دو فرمول متفاوت برای اینباندهای قدیمی/میان‌بر داشتند. کارت به‌درستی WS را نشان می‌داد ولی چند لحظه بعد فیلد واقعی که ارسال می‌شد، خاموش به TCP برمی‌گشت. الان هر دو از یک تابع مشترک (`deriveManualParts`) استفاده می‌کنند و دیگر امکان اختلاف ندارند.
- **دکمه‌ی «بروزرسانی همه» برای اینباندها:** یک دکمه‌ی جدید در بالای صفحه‌ی اینباندها اضافه شد که با یک کلیک، هم لیست اینباندها/دسته‌ها و هم آمار کلی داشبورد را بروزرسانی می‌کند.
- **الگوی سفارشی نام کانفیگ در ساب (تنظیمات → Subscription Template):** یک بخش جدید در تنظیمات اضافه شد که مالک پنل می‌تواند انتخاب کند نام هر کانفیگ داخل اپ کاربر شامل چه مواردی باشد: نام، حجم اختصاص‌یافته، شناسه (ID) و/یا نام اینباند — با پیش‌نمایش زنده. پیش‌فرض دقیقاً مثل قبل (فقط نام) است تا هیچ ساب فعلی به‌هم نریزد.
- طراحی لیست اینباندها (همان جدول موجود، بدون تغییر ستون‌ها یا قابلیت‌ها) ارتقا یافت: هاور با نوار رنگی کناری، چیپ‌های پروتکل/ترنسپورت واضح‌تر، دکمه‌های عملیات با هاور نرم، کارت‌های آمار بالای صفحه با گرادیان و هاور ظریف.
- تمام تغییرات با `node --check` روی هر سه بلوک اسکریپت (لاگین + دو بخش داشبورد) و `python -m py_compile` روی `main.py` و `pages.py` بررسی شدند.


- **سابسکریپشن یکپارچه:** حالا فقط یک لینک `/sub/{uuid}` وجود دارد. کلاینت‌های VPN (v2rayNG، v2rayN، Hiddify، Clash، sing-box و…) طبق حالت قبل کانفیگ خام دریافت می‌کنند؛ اگر همان لینک را داخل مرورگر باز کنید، به‌صورت خودکار به صفحه‌ی نمایش حرفه‌ای (`/subscription/{uuid}`) هدایت می‌شوید. دیگر نیازی به دو لینک جدا برای اپ و برای مشاهده نیست.
- **رفع باگ اصلی ترجمه انگلیسی:** موتور ترجمه داشبورد قبلاً با جایگزینی زیررشته‌ای بدون رعایت مرز کلمه کار می‌کرد؛ همین باعث می‌شد کلماتی مثل «کلید» به‌اشتباه به «Totalید» تبدیل شوند (چون «کل» زیررشته‌ای از آن است). این باگ با یک الگوریتم جایگزینی مرز-امن رفع شد.
- نزدیک به ۷۰ متن که اصلاً در دیکشنری ترجمه نبودند (پیام‌های Toast، دیالوگ‌های تایید حذف، برچسب‌های Inbound Builder و Client Manager، ماتریس دسترسی ادمین‌ها و…) اضافه شدند.
- دیالوگ‌های تایید (`confirm()` برای حذف کانفیگ/کلاینت/ادمین/دسته/گروه/پلن و پاک‌کردن خطاها) قبلاً هرگز ترجمه نمی‌شدند؛ حالا در حالت English به‌درستی نمایش داده می‌شوند.
- **رفع هنگ موبایل در صفحه‌ی ورود بدون خاموش‌کردن «سیاره»:** قبلاً برای رفع کندی روی گوشی، تمام انیمیشن‌ها از جمله چرخش حلقه/سیاره خاموش می‌شد. الان فقط لایه‌های سنگین (بلور/گرادیان چرخان پس‌زمینه، Glow، Scanline) روی موبایل خاموش می‌مانند و چرخش سیاره چون فقط `transform` است و برای GPU تقریباً رایگان است، روی گوشی هم ادامه پیدا می‌کند.
- تمام تغییرات با `node --check` (سه بلوک اسکریپت لاگین و داشبورد) و `python -m py_compile` روی `main.py` و `pages.py` بررسی شدند تا چیزی نشکند.


- باگ `d.dir` در تغییر زبان که باعث توقف JavaScript می‌شد رفع شد.
- موتور ترجمه به یک سیستم واحد و stateful تبدیل شد.
- ترجمه فقط محدود به `#app` نیست؛ Drawer و Toastهای داینامیک هم در حالت انگلیسی ترجمه می‌شوند.
- مشکل از دست رفتن متن فارسی هنگام برگشت EN → FA رفع شد.
- تغییر متن‌های داینامیک هنگام فعال بودن English به‌درستی دوباره ترجمه می‌شود.
- `placeholder`، `title` و `aria-label` نیز پایدارتر مدیریت می‌شوند.
- MutationObserver به یک نمونه کاهش یافت و از loop ترجمه جلوگیری شد.
- تست compile پایتون، syntax جاوااسکریپت و routeهای اصلی دوباره انجام شد.
# VodiWalker 27.0.0 — Control Center Upgrade

- داشبورد و Control Center ارتقا داده شد؛ مدیریت Inbound/Client و ابزارهای مدیریتی حفظ و یکپارچه شدند.
- امکان تغییر نام کاربری و رمز حساب واردشده اضافه شد؛ username در state پایدار ذخیره می‌شود.
- نمایش username واقعی مالک در Admin Manager اصلاح شد.
- حالت English داشبورد به LTR واقعی ارتقا یافت و برای محتوای پویا MutationObserver ترجمه‌ی UI را حفظ می‌کند.
- صفحه Login دست‌نخورده باقی ماند.
- مسیر/دکمه‌ی «تمدید از ربات» از Subscription Portal حذف شد.
- همه‌ی فایل‌های Python با py_compile بررسی شدند و import کامل پروژه نیز تست شد.
# تغییرات نسخه 22

- بازطراحی کامل Subscription Portal
- Appearance Studio حرفه‌ای با فونت‌های Estedad، IBM Plex Sans Arabic، Shabnam و Yekan Bakh
- اضافه شدن ۱۰ Accent Color
- اضافه شدن Font Size، Radius، Glow، Motion و Wide Sidebar
- رفع مشکل ذخیره نشدن انتخاب فونت
- تبدیل لیست Inbound به کارت‌های مدیریتی حرفه‌ای
- اضافه شدن ابزارهای Control Center Pro
- حفظ سازگاری APIهای Subscription و Railway

## نسخه 25.0.0 — Message Center & Secure Login
- اضافه شدن «مرکز پیام» مستقل برای خطاها و هشدارها.
- ثبت خطاهای Backend و خطاهای JavaScript/Unhandled Promise در پنل.
- فیلتر خطا، هشدار و خطاهای مرورگر + پاک‌سازی امن لاگ خطا.
- بروزرسانی خودکار مرکز پیام و شمارنده خطا در ناوبری.
- ارتقای گرافیک صفحه ورود با لایه‌های نور، Grid، Scanline، وضعیت امنیتی و کارت دسترسی حرفه‌ای.
- بهبود گزارش خطاهای API برای نمایش پیام دقیق‌تر به کاربر.
- تست Health/Login/API Error انجام شد.

### تکمیل حرفه‌ای Builder — v27.1
- صفحه‌ی واسط قدیمی روت حذف شد؛ آدرس `/` مستقیماً به `/login` می‌رود و بعد از ورود به `/dashboard` هدایت می‌شود.
- Builder اینباند کاملاً تفکیک شد: **Base Protocol** در یک بخش مستقل، **Transport/Network** در بخش مستقل و **Security** در بخش مستقل.
- پروتکل‌های پایه‌ی Builder اکنون شامل VLESS، VMess، Trojan و Shadowsocks هستند.
- Transportها به‌صورت کارت‌های مستقل TCP / WebSocket / gRPC / XHTTP نمایش داده می‌شوند.
- برای Shadowsocks محدودیت TCP-only در UI اعمال شده و فیلد Method/Password اختصاصی دارد.
- فیلدهای پیشرفته فقط متناسب با انتخاب‌های کاربر نمایش داده می‌شوند و خلاصه‌ی نهایی قبل از ساخت قابل مشاهده است.

## v27.1.0 — رفع باگ زبان و پایداری UI
- موتور ترجمه داشبورد یکپارچه شد؛ دو مترجم هم‌زمان که باعث خراب شدن متن اصلی هنگام EN → FA می‌شد حذف شد.
- بازگشت کامل EN → FA برای متن‌های ایجادشده به‌صورت داینامیک پایدار شد.
- Optionهای فرم‌ها، placeholder، title و aria-label نیز در تغییر زبان مدیریت می‌شوند.
- MutationObserver فقط یک نمونه دارد و از ترجمه دوباره/تو در تو جلوگیری می‌شود.
- مجموعه عبارت‌های رابط کاربری و Inbound Builder تکمیل‌تر شد.
- تست Syntax پایتون، Syntax جاوااسکریپت و Routeهای اصلی انجام شد.

## v27.2.0 — Final stability pass
- رفع باگ اصلی تغییر زبان داشبورد: `data-i18n` و عنوان صفحه دیگر متن اصلی خود را از دست نمی‌دهند.
- جلوگیری از حلقه‌ی MutationObserver هنگام ترجمه‌ی متن، placeholder، title و aria-label.
- تغییر زبان FA ↔ EN برای عناصر ثابت و داینامیک پایدارتر شد.
- هنگام تغییر رمز ادمین فرعی، نشست‌های قبلی همان حساب باطل می‌شوند.
- شمارش Inbound/Client در Diagnostics دقیق‌تر شد.
- نسخه برای این انتشار به 27.2.0 ارتقا یافت.
